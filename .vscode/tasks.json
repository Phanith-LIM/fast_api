{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FastAPI",
            "type": "shell",
            "command": "source venv/bin/activate && echo venv activated && uvicorn main:app --reload",
        }
    ]
}